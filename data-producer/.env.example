# AWS Configuration
AWS_REGION=us-east-1
STREAM_NAME=dashboard-updates
INTERVAL_MS=100

# Optional: AWS credentials (if not using AWS CLI profile)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
